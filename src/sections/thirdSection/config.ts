/** Initial counter value at mount. */
export const INITIAL_COUNT = 0

/** How many clicks the player needs to solve the challenge. */
export const TARGET_COUNT = 5

export const CODE_RIGHT = `const [count, setCount] = useState(0)
setCount(c => c + 1) // re-renders`
